```jsx
<RadioButton
  name="name"
  value="value"
  onClick={() => { alert('clicked') }}
  label={<span style={{color: 'red', width: 'auto', fontWeight: 700}}>Click Me</span>}
  tabIndex={0}
  key="key"
/>
```
