import React from 'react'
import { Select } from './Select'
import styles from './SelectCompact.scss'

export const SelectCompact = (props) => {
  return <Select className="SelectCompact" {...props} />
}
